#include "main.h"

/**
 * _islower - checks lowercase characters
 * @c: has value to be compared
 * Return: 0 (Success)
 */
int _islower(int c)

{

if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}

}


